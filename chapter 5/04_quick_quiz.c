#include <stdio.h>
#include <math.h>

int main(){
    int a = 5;
    printf("the area of the square is %f",pow(a,2));
    return 0;
}
// as it gives double data type, we need to use %f format specifier