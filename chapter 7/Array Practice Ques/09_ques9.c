// Delete Element from Specified Position
#include <stdio.h>

int main(){
    int arr[10],n,pos;
    printf("enter number of elements");
    scanf("%d",&n);
    for (int i= 0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    
    return 0;
}