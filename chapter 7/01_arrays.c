#include <stdio.h>

int main(){
    int marks[90];
    marks[0] =45;
    marks[1] = 49;
    // we can go all the way till marks[89]
    printf("value of marks 0 and marks 1 is %d and %d", marks[0],marks[1]);
    return 0;
}