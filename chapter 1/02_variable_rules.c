#include <stdio.h>

int main(){
    int abhey;  // variable cannot start with capital letter
    int _sharma; // special characters are not allowed 
    int parneet;  // can be start with underscore
    int virk_; // cannot start with digit 
    return 0;
}